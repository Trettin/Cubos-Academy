const nomes = ["maria", "fernanda", "gabriel", "joão", "josé"]

for (let i=0; i< nomes.length;i++) {
    if (nomes[i]==="joão") {
        console.log(`O nome joão encontra-se no índice ${i}.`)
        break;
    }
}