import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    width: 300,
    height: "auto",
  },
  media: {
    paddingTop: "100%",
    backgroundSize: "contain",
  },
  content: {
    textAlign: "left",
  },
}));
